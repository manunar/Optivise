/**
 * API Route: /api/admin/leads/[id]
 * Gestion d'un lead spécifique - TODO: Implémenter
 */

import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ 
    success: true,
    data: null
  });
}

export async function PUT() {
  return NextResponse.json({ 
    success: true,
    data: null
  });
}