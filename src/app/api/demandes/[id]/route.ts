/**
 * API Route: /api/demandes/[id]
 * Gestion d'une demande spécifique - TODO: Implémenter
 */

import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ 
    success: true,
    data: null
  });
}